# CV Training Dataset — Exploratory Data Analysis

**Dataset location:** `data/cv_train/survivor/`  
**Generated by:** `scripts/train_survivor_detector.py --altitude-aware`  
**Image size:** 640×640 px  
**Format:** YOLO detection (`.jpg` images + `.txt` label files, class 0 = person)  
**Generation mode:** Altitude-aware (survivor size derived from camera physics at 20–50 m)

> **Document history:** Sections 1–14 describe the original 640px, 2,400-image
> drone generation and are kept for provenance. Section 15 covers the
> **1280px, 6,800-image retrain** that superseded it as the deployed weight
> (`survivor_yolov8s_1280.pt`), and Section 16 covers all **real-image**
> datasets (HIT-UAV, HERIDAL, COCO, FLAME 3, real Malibu-terrain composites)
> added for sim-to-real validation and fine-tuning. See
> `docs/real_data_eval_report.md` for the full sim-to-real analysis and
> `docs/cv_dataset_composition_report.md` for exact hard-negative/decoy counts.

---

## 1. Dataset Size

| Split | Images | Survivor annotations | Avg survivors/image |
|-------|:------:|:-------------------:|:-------------------:|
| Train | 2,000 | ~3,460 | ~1.73 |
| Val | 400 | ~700 | ~1.75 |
| **Total** | **2,400** | **~4,160** | **~1.73** |

---

## 2. Image Composition Breakdown

| Image type | Count | Percentage | Description |
|------------|:-----:|:----------:|-------------|
| With survivors | 798 | 88.7% | 1–3 survivors composited on background |
| Negative-only (no person) | 102 | 11.3% | Pure background — teaches "terrain ≠ person" |
| With fire/smoke | ~585 | ~65% | Procedural wildfire effects overlaid |
| With hard-negative decoys | ~180 | ~20% | Non-human objects composited (no label) |
| Edge/boundary placements | 379 | 24.2% | Survivor partially outside frame |

### Survivors per image (train split):

| Survivors | Images | Percentage |
|:---------:|:------:|:----------:|
| 0 | 88 | 11.0% |
| 1 | 262 | 32.8% |
| 2 | 230 | 28.8% |
| 3 | 220 | 27.5% |

---

## 3. Altitude Distribution

Each image simulates a specific drone flight altitude, sampled uniformly from the operational envelope. Survivor pixel size and resolution blur are derived from the altitude via the pinhole camera model.

| Metric | Value |
|--------|-------|
| Min altitude | 20.1 m |
| Max altitude | 49.9 m |
| Mean altitude | 34.9 m |
| Min GSD (ground sample distance) | 0.040 m/px (at 20 m) |
| Max GSD | 0.099 m/px (at 50 m) |
| Images with altitude metadata | 900 (100%) |

### Oblique (Side-Angle) Drone Views

In addition to nadir (straight-down) views, 25% of generated images simulate an
**oblique camera** tilted 15–45° from nadir (`--oblique-frac`, default 0.25).
Survivors in oblique images appear elongated/partially upright rather than as
flat circular blobs:

- Apparent height follows the projection physics:
  `height = person_height × sin(tilt) + body_width × cos(tilt)`
- Each image's metadata sidecar records `"oblique"` and `"tilt_deg"`.
- The simulation adapter mirrors this with a `camera_tilt_deg` parameter
  (`--cv-camera-tilt` in trajectory export), so oblique-trained models are
  exercised at deployment with the same geometry.

Retrained drone detector including oblique views (2,000 train / 400 val, 50
epochs): Precision 0.863, Recall 0.778, mAP50 0.818.

---

## 4. Source Assets

### SARD Survivor Cutouts (positive class)

| Metric | Value |
|--------|-------|
| Total extracted | 76 |
| Accepted for training | 54 |
| Rejected (poor segmentation) | 22 |
| Original width range | 24–338 px |
| Original height range | 32–383 px |
| Mean aspect ratio (H/W) | 1.91 |

**Inferred pose distribution (from original cutout aspect ratio):**

| Pose | Count | Percentage | Aspect ratio |
|------|:-----:|:----------:|:------------:|
| Standing/upright | 16 | 30% | H/W > 2.0 |
| Crouching/sitting | 33 | 61% | 0.8 ≤ H/W ≤ 2.0 |
| Lying down | 5 | 9% | H/W < 0.8 |

Note: SARD images are captured from UAVs during search-and-rescue exercises. The high proportion of crouching/sitting reflects realistic SAR scenarios (injured, resting, taking shelter).

### NAIP Background Tiles

| Metric | Value |
|--------|-------|
| Training tiles | 67 (Malibu Creek, CA — large area) |
| Validation tiles | 4 (Malibu Creek, CA — different geographic sector) |
| Additional eval regions | Arizona desert (4), PNW forest (2), Texas grassland (4) |
| Source | USDA NAIP |
| Resolution | 0.6 m/px |
| Augmentation | Random crop, 90° rotation, color jitter, flip |
| Geographic split | Train and val from **different geographic areas** (unseen terrain evaluation) |

### Hard-Negative Decoys (three classes)

All decoys go through the identical composite pipeline as survivors (harmonize →
erode → blur) but carry no label, and are placed so they never overlap a labeled
survivor. The two human-scale classes were added after reviewer feedback: vehicles
alone are large and mostly white/gray, so a model could shortcut on "colourful
human-sized blob = person" or on compositing artifacts. Human-scale decoys match
survivors in size band, clothing-like colour and contrast, leaving silhouette as
the only separator. Per-image decoy type/bbox/size are stored in the metadata
(`"decoys"`), and `scripts/eval_decoy_fp.py` reports the false-positive rate per
decoy class.

| Class | Source | Physical size | Share |
|-------|--------|---------------|------:|
| Vehicles | VisDrone crops (500 RGBA assets, 26–226 px) | cars 3.5–5.2 m (70%), vans 5.2–8 m (22%), bus/truck 8–13 m (8%) | ~50% |
| Animals | Procedural (body + head blob, fur palette) | 0.5–1.8 m long axis | ~25% |
| Colourful objects | Procedural (tarp/jacket/gear, saturated 1–2 tone) | 0.5–1.8 m long axis | ~25% |
| Object types | Cars, vans, trucks, motorcycles (top-down crops) |

These are composited with the **same pipeline** as survivors (color harmonization, edge erosion, resolution blur) but carry **no label** — they are the hard-negative signal that forces the model to distinguish human shape from vehicle shape.

---

## 5. Bounding Box Size Distribution

### Width (pixels)

| Range | Count | Percentage | Altitude equivalent |
|-------|:-----:|:----------:|---------------------|
| Tiny (<25 px) | 299 | 19.1% | ~45–50 m altitude |
| Small (25–50 px) | 1,024 | 65.4% | ~25–45 m altitude |
| Medium (50–75 px) | 243 | 15.5% | ~20–25 m altitude |
| Large (>100 px) | 0 | 0% | N/A (not generated) |

| Statistic | Width (px) | Height (px) |
|-----------|:----------:|:-----------:|
| Min | 15 | 13 |
| Max | 75 | 299 |
| Mean | 37 | 69 |
| Median | 34 | 57 |

**The size distribution now matches what the drone actually sees at 20–50 m altitude.** No unrealistically large survivors (>100 px) are generated — this was the key improvement over the old uniform-random mode.

---

## 6. Composited Survivor Pose Distribution

Pose is inferred from the bounding box aspect ratio of placed survivors:

| Pose | Count | Percentage |
|------|:-----:|:----------:|
| Standing/upright (H/W > 2.0) | 450 | 29% |
| Crouching/sitting (0.8–2.0) | 986 | 63% |
| Lying down (H/W < 0.8) | 130 | 8% |

The distribution reflects the source SARD asset mix. Crouching/sitting dominates because SAR scenarios involve injured or sheltering people.

---

## 7. Spatial Placement

| Metric | Value |
|--------|-------|
| Center X (mean ± std) | 0.494 ± 0.276 |
| Center Y (mean ± std) | 0.502 ± 0.260 |
| Edge placements (within 10% of border) | 379 (24.2%) |

Placements are approximately uniform across the image, with ~24% at the edges (boundary-aware training ensures the model handles partially-visible survivors).

---

## 8. Data Augmentation Applied During Generation

| Augmentation | Parameters | Purpose |
|-------------|-----------|---------|
| Altitude sampling | Uniform 20–50 m | Realistic size distribution |
| Color harmonization | 25–55% blend toward local BG mean | Reduce color mismatch artifact |
| Alpha-edge erosion | 0–2 px erosion + Gaussian feather | Remove GrabCut hard-edge cue |
| Altitude-scaled blur | GSD-derived radius (0.5–1.3 px) | Match effective resolution at altitude |
| NAIP rotation | Random 90° (0/90/180/270) | Increase terrain diversity |
| NAIP color jitter | ±15 per channel, 0.85–1.15 brightness | Simulate lighting variation |
| Horizontal flip | 50% probability | Double effective background variety |
| Fire/smoke overlay | Burns under, smoke over survivors | Train occlusion robustness |
| Heavy smoke columns | 20% of fire images, directly on survivors | Extreme occlusion case |
| Size jitter | ±30% around physics-derived base | Body pose/orientation variation |

---

## 9. Known Limitations

1. **Only 54 unique survivor appearances** — limited pose/clothing diversity. More SARD frames or other aerial person datasets would help.
2. **No real wildfire imagery** — fire/smoke effects are procedural (synthetic). Real smoke has more complex structure.
3. **Single class (person)** — no distinction between poses (standing vs lying) in the labels.
4. **All survivors are static cutouts** — no motion blur or temporal variation.
5. **Small objects dominate** — 84% of annotations are <50 px width, close to YOLO's detection floor. This is realistic but challenging.

---

## 10. UGV Ground-Robot Dataset

**Dataset location:** `data/cv_train/ugv/{front,mast}/`  
**Generated by:** `scripts/train_ugv_detector.py`  
**Image size:** 640×640 px  
**Format:** YOLO detection (class 0 = person)

### Camera Models

| Camera | Mount height | FOV | Range | Base YOLO | Purpose |
|--------|:----------:|:---:|:-----:|:---------:|---------|
| Front | 0.8 m | 70° | 5–30 m | yolov8s | Forward detection while driving |
| Mast | 3–5 m | 60° | 3–15 m | yolov8n | Visual confirmation on arrival |

### Key Differences from Drone Dataset

| Aspect | Drone | UGV Front | UGV Mast |
|--------|-------|-----------|----------|
| View angle | Nadir (top-down) | Horizontal (ground-level) | Oblique (~45° down) |
| Person orientation | Foreshortened/circular | Upright (full height) | Partially foreshortened |
| Person pixel height | 15–75 px | 30–350 px | 40–250 px |
| Occlusion type | Smoke/fire | Vegetation + smoke | Smoke |
| Background | NAIP aerial | NAIP tight-crop + procedural | NAIP tight-crop + procedural |
| Range model | Altitude (20–50 m vertical) | Horizontal distance (5–30 m) | Slant range (3–15 m ground) |

### Front Camera Physics

At the front camera (0.8 m height, 70° horizontal FOV, 640 px image):

| Range | Person height (px) | Person width (px) |
|:-----:|:-----------------:|:----------------:|
| 5 m | ~240 | ~75 |
| 10 m | ~80 | ~25 |
| 20 m | ~40 | ~13 |
| 30 m | ~27 | ~8 |

Triangular distribution biased toward closer ranges (detection is most critical when survivor is nearby).

### Mast Camera Physics

At the mast camera (4 m height, 60° tilt, 60° FOV, 640 px image):

| Range | Person height (px) | Person width (px) |
|:-----:|:-----------------:|:----------------:|
| 3 m | ~150 | ~50 |
| 8 m | ~65 | ~22 |
| 15 m | ~35 | ~12 |

Foreshortening reduces apparent person height compared to pure horizontal view.

### UGV-Specific Augmentations

| Augmentation | Parameters | Purpose |
|-------------|-----------|---------|
| Range sampling (front) | Triangular 5–30 m | Bias toward close targets |
| Range sampling (mast) | Uniform 3–15 m | Even coverage of confirmation zone |
| Mast height jitter | Uniform 3–5 m | Simulate varying mast extension |
| Vegetation occlusion | 50% prob, 3–12 strokes | Ground-level brush partial hiding |
| Range-dependent blur | 0.04 * range_m px | Atmospheric + focus blur at distance |
| Ground-level fire | 40% of images | Fire near ground, smoke drifts up |
| Color harmonization | 15–40% blend | Match sprite tone to background |

### Dataset Size

| Split | Camera | Images | Persons | Avg persons/image |
|-------|--------|:------:|:-------:|:----------------:|
| Train | Front | 1,500 | ~2,625 | ~1.75 |
| Val | Front | 300 | ~525 | ~1.75 |
| Train | Mast | 1,500 | ~2,250 | ~1.50 |
| Val | Mast | 300 | ~450 | ~1.50 |

Validation uses NAIP tiles from a **different geographic area** than training (unseen terrain).

### Training Results (40 epochs, unseen terrain validation)

| Model | Precision | Recall | mAP50 | mAP50-95 | Weight file |
|-------|:---------:|:------:|:-----:|:--------:|-------------|
| Front (yolov8s) | 0.930 | 0.863 | 0.942 | 0.390 | `models/ugv_front_yolov8s.pt` |
| Mast (yolov8n) | 0.852 | 0.800 | 0.811 | 0.305 | `models/ugv_mast_yolov8n.pt` |

The front camera achieves strong mAP50 (0.94) with the larger yolov8s architecture trained on 2.5x more data. Both models validate on geographically unseen terrain (different NAIP tile areas), confirming generalization.

---

## 11. Perception Evaluation Results

Evaluated on synthetic test scenes (10 trials per scenario, 640×640 px). Full details in `docs/cv_perception_eval.md`.

| Metric | Value |
|--------|-------|
| **Overall recall** | 0.935 |
| **Overall precision** | 0.773 |
| **mAP50** | 0.907 |
| **FP rate (empty backgrounds)** | 0.05 / frame |
| **Mean spatial error** | 3.6 px |
| **Temporal stability** | 100% (deterministic across repeated runs) |
| **Inference time (CPU)** | ~480 ms / frame |

### Performance by Condition

| Condition | Recall | Precision |
|-----------|:------:|:---------:|
| Clean (no occlusion) | 1.00 | 0.66 |
| Light smoke | 1.00 | 0.80 |
| Heavy smoke | 1.00 | 0.83 |
| Fire + smoke | 0.83 | 0.89 |
| Small targets (30 px) | 0.83 | 0.94 |
| Large targets (120 px) | 1.00 | 0.63 |

Fire/smoke occlusion at small sizes (30 px) is the hardest condition (recall drops to 0.60–0.70). This matches the deployment challenge — small survivors partially hidden by smoke at high altitude.

---

## 12. CV Pipeline Improvements (Post-Training)

Beyond training data improvements, the CV pipeline includes runtime enhancements for improved detection quality during deployment:

### 11.1 UGV-Specific Model Routing

The `SimulationCvAdapter` automatically selects the appropriate detector based on the agent type and camera mode:

| Agent | Camera mode | Model used | When |
|-------|------------|-----------|------|
| Drone | — | `yolov8s` (drone) | All aerial detections |
| UGV | `front` | `ugv_front_yolov8s` | Forward driving detection |
| UGV | `mast` | `ugv_mast_yolov8n` | Close-range confirmation |
| UGV | `auto` | Selects front or mast by range | Default behavior |

Falls back to drone model if UGV-specific weights are unavailable.

### 11.2 Multi-Object Tracking (ByteTrack)

Temporal consistency across frames using `supervision.ByteTrack`:

| Parameter | Value | Purpose |
|-----------|-------|---------|
| Track activation threshold | 0.25 | Minimum confidence to initiate a track |
| Lost track buffer | 5 frames | How long to keep a lost track alive |
| Minimum matching threshold | 0.8 | IoU threshold for re-identification |
| Min hits to confirm | 2 | Detections needed before track is "confirmed" |

**Effect:** Reduces false positives by requiring temporal persistence. A detection is only marked as a confirmed survivor after appearing in at least 2 frames. Transient false positives (single-frame artifacts) are filtered out.

### 11.3 Test-Time Augmentation (TTA)

Enabled via `augment=True` in Ultralytics predict. Runs inference on:
- Original image
- Horizontally flipped
- Multi-scale (0.83×, 1.0×, 1.17×)

Results are merged via NMS. TTA improves recall by ~3–5% at the cost of ~3× inference time. Recommended for offline trajectory export and evaluation; disabled for real-time operation.

### 11.4 Adaptive Confidence Thresholds

Dynamically adjusts the detection confidence threshold based on drone altitude:

| Altitude | Confidence threshold | Rationale |
|----------|:-------------------:|-----------|
| ≤ 20 m (low) | 0.35 (higher) | Survivors are large/clear, fewer false positives |
| ≥ 50 m (high) | 0.15 (lower) | Survivors are tiny, allow more candidates through |
| 20–50 m | Linear interpolation | Smooth transition |

**Effect:** At high altitude, the model must be more permissive (tiny targets have lower confidence). At low altitude, we can be strict (larger targets should score high). This balances precision and recall across the operating envelope.

### 11.5 Cross-Terrain Evaluation Regions

Downloaded NAIP tiles from geographically diverse biomes for robust evaluation:

| Region | Description | Tiles | Challenge |
|--------|-------------|:-----:|-----------|
| Arizona Desert | Sonoran Desert — sparse vegetation, sandy/rocky | 4 | High contrast, minimal occlusion |
| PNW Forest | Olympic National Forest — dense conifer canopy | 2 | Heavy shadows, canopy occlusion |
| Texas Grassland | Hill Country — open grass with scattered brush | 4 | Uniform texture, subtle targets |

These regions are used for **cross-terrain generalization testing** — evaluating whether models trained on California (Malibu) backgrounds can detect survivors in unseen biomes.

### Usage (CLI flags for `scripts/export_trajectories.py`)

```
--cv-augment          Enable TTA (slower, higher recall)
--cv-adaptive-conf    Enable altitude-adaptive confidence
--cv-tracking         Enable ByteTrack multi-object tracking
--cv-tracking-min-hits N   Frames required to confirm a track (default: 2)
--detection-mode MODE     cv | thermal | cv+thermal (default: cv)
```

---

## 13. Simulated Thermal Infrared (TIR) Sensor & Fusion

**Module:** `detection/thermal_model.py`  
**Tests:** `tests/test_thermal_model.py` (15 tests)

A physics-based simulated thermal sensor allows comparison of detection modalities without real thermal hardware. The thermal model operates on simulator state directly (no image generation).

### 13.1 Thermal Detection Physics

| Parameter | Value | Physical meaning |
|-----------|-------|-----------------|
| Body temperature | 310 K (37°C) | Human thermal signature |
| Ambient (no fire) | 293 K (20°C) | Normal ground background |
| Active fire ground | 450 K (177°C) | Near flame front |
| Burned/cooling ground | 330 K (57°C) | Post-fire, approaching body temp |
| NETD (sensitivity) | 0.05 K | Minimum detectable ΔT |
| Crossover threshold | 2.0 K | Below this, detection fails |
| Smoke attenuation | 0.4 (vs 1.4 for visible) | TIR penetrates smoke better |
| Smoke transmittance floor | 0.70 | Even in heavy smoke |
| Base detection probability | 0.92 | At optimal ΔT and close range |
| False positive rate | 0.08 / frame | Warm animals, embers, heated rocks |

### 13.2 Thermal Crossover Problem in Wildfire

The critical physics issue: near active fire or on recently burned ground, ambient temperature approaches body temperature (37°C), causing ΔT → 0:

| Condition | Ground temp | ΔT from body | Detection probability |
|-----------|:-----------:|:------------:|:--------------------:|
| Normal (clear) | 20°C | **17°C** | ~0.74 (high) |
| Light smoke | 20°C | **17°C** | ~0.60 (smoke attenuation only) |
| Burned ground (cooling) | 57°C | 20°C | ~0.74 (still OK, body is cooler) |
| Burned ground (warm) | ~37°C | **≈0°C** | **<0.05** (crossover!) |
| Active fire | 177°C+ | 140°C+ | ~0.74 (inverted contrast — body is cold spot) |

**Key insight:** Thermal works *between* fire zones (smoke-only areas) but **fails** on recently-burned ground where temperatures equal body temp (the exact area where survivors are most likely found after fire passes).

### 13.3 Detection Modes

| Mode | Command | What runs | Best for |
|------|---------|-----------|----------|
| `cv` | `--detection-mode cv` | YOLOv8 on rendered RGB image | Clear daytime, default |
| `thermal` | `--detection-mode thermal` | Simulated TIR sensor only | Smoke/night scenarios |
| `cv+thermal` | `--detection-mode cv+thermal` | Both → union fusion | Maximum recall |
| `motion` | `--detection-mode motion` | Temporal frame differencing | Moving targets only |
| `cv+motion` | `--detection-mode cv+motion` | CV + motion confirmation boost | Reduce FP confidence |

### 13.4 Motion Detection (`detection/motion_detector.py`)

Frame differencing detects new objects appearing between consecutive rendered drone frames:

1. Convert frames to grayscale
2. Compute absolute pixel difference from previous frame
3. Threshold to binary change mask
4. Find connected components (blobs)
5. Filter by survivor-plausible size (100–15,000 px)

**Limitations specific to wildfire SAR:**
- Survivors are mostly **static** (injured/unconscious) — no movement to detect
- Fire/smoke **movement creates false positives** (flickering, drifting smoke)
- Requires drone to be **moving** (static hover = no frame difference)
- First frame always returns empty (no previous to compare)

### 13.5 Fusion Strategies

| Strategy | Mode | Rule | Effect |
|----------|------|------|--------|
| **CV+Thermal Union** | `cv+thermal` | Either sensor detects → candidate | Higher recall |
| **CV+Thermal Intersection** | configurable | Both must detect → confirmed | Highest precision |
| **CV+Thermal Weighted** | configurable | 0.6×CV_conf + 0.4×TH_conf | Balanced |
| **CV+Motion Boost** | `cv+motion` | Motion confirms CV → confidence +0.15 | Fewer weak FPs |
| **CV+Motion Confirm** | configurable | Only motion-confirmed CV passes | Very high precision |

### 13.6 Measured Performance Comparison

Results from `scripts/compare_detection_modes.py` (100 trials per scenario, 3 survivors):

| Scenario | CV | Thermal | Motion | CV+Thermal | CV+Motion | Best |
|----------|:--:|:-------:|:------:|:----------:|:---------:|------|
| Clear | **95.3%** | 75.0% | 33.3% | **98.8%** | 95.3% | CV+Th |
| Light smoke | 56.7% | 62.0% | 33.3% | **83.5%** | 56.7% | CV+Th |
| Heavy smoke | 56.7% | 54.3% | 33.3% | **80.2%** | 56.7% | CV+Th |
| Active fire | 53.0% | 54.3% | 33.3% | **78.5%** | 53.0% | CV+Th |
| Burned ground | **64.7%** | 3.0% | 33.3% | **65.8%** | 64.7% | CV+Th |
| Mixed | 54.7% | 4.3% | 33.3% | **56.6%** | 54.7% | CV+Th |

**Precision** remains >99% across all modes (false positive rate ≤0.02/frame).

**Key findings:**

1. **CV+Thermal fusion wins every scenario** — complementary failure modes
2. **Thermal crossover confirmed** — on burned ground (ΔT≈0), thermal collapses to 3% recall; CV maintains 65% and rescues the fusion
3. **In smoke**, both sensors individually degrade to ~55%, but fusion lifts to 80% (independent failures)
4. **Motion is the weakest modality** (33.3% recall) — survivors in wildfire are mostly static, and frame differencing requires visible appearance change between consecutive drone frames
5. **CV+Motion (boost mode)**: same recall as CV alone, but confirmed detections get higher confidence scores — useful for downstream prioritization, not for recall improvement
6. **No training data needed for thermal or motion** — both are physics/signal-processing based

**Conclusion:** CV+Thermal is the recommended fusion for wildfire SAR. Motion detection adds negligible value because survivors are immobile. CV alone remains the strongest single modality for daytime operations.

### 13.7 Generated TIR Image Dataset & Trained Thermal Detector

Beyond the physics-based probability model, a **simulated thermal image
dataset** was generated (`scripts/generate_thermal_dataset.py`) using the
thermal renderer (`detection/thermal_renderer.py`):

| Property | Value |
|----------|-------|
| Total images | 1,200 (1,000 train / 200 val) |
| Format | 512×512 grayscale PNG + YOLO labels + metadata JSON |
| Location | `data/cv_train/thermal/` |
| Scenarios | clear, fire, burned, smoke, fire+smoke, burned+smoke |
| Negatives | 10% (no survivors) |
| Survivor rendering | Gaussian heat blob (310 K body vs 293 K ambient), radius scales with altitude |
| Normalization | Scene-adaptive contrast (AGC), like a real thermal camera |

A YOLOv8-nano trained on this dataset (30 epochs) reached **Precision 1.000,
Recall 0.963, mAP50 0.965, mAP50-95 0.843** (`models/thermal_yolov8n.pt`).
The near-perfect scores reflect the simplicity of the simulated task; real TIR
imagery would be substantially harder.

The trained detector is wired into the simulation as an alternative thermal
backend: `SimulationCvAdapter(thermal_detector="yolo")` renders a TIR frame
each step and runs the model on it, feeding the same `cv+thermal` fusion
pipeline as the physics backend (`--thermal-detector yolo` in trajectory
export).

---

## 14. Zenodo Publication

Published: **DOI 10.5281/zenodo.21226010** (CC-BY 4.0, 1.4 GB). Contents:
- All drone images + labels + altitude metadata JSONs (train/val) — 2,400 images (25% oblique views)
- All UGV front/mast images + labels + metadata JSONs (train/val) — 3,600 images
- The simulated thermal TIR dataset (train/val) — 1,200 images
- The 54 accepted SARD cutouts (with provenance)
- The 500 VisDrone decoy crops
- NAIP tiles: training set (67 tiles, Malibu), validation set (4 tiles, different area), and 3 evaluation regions (10 tiles total)
- All generation scripts + config JSON for full reproducibility
- 5 trained model weights (drone, drone_naip, UGV-front, UGV-mast, thermal)
- This EDA document as a datasheet

Not yet included in the published record (candidates for a follow-up
version): the 1280px/6,800-image drone retrain (§15), the real-image
evaluation/fine-tune sets and the real-Malibu-terrain UGV composites (§16),
and the fine-tuned weights (`survivor_yolov8s_1280.pt`,
`survivor_heridal_yolov8s_1280.pt`, `ugv_front_yolov8s_real.pt`,
`thermal_real_yolov8n.pt`).

---

## 15. 1280px Retrain — Deployed Drone Model

**Dataset location:** `data/cv_train/survivor_6k/`  
**Generated by:** `scripts/train_survivor_detector.py` (native 1280px generation)  
**Image size:** 1280×1280 px (vs. 640×640 for the original set in §1–13)  
**Weight file:** `models/survivor_yolov8s_1280.pt` — the model actually
deployed in the simulation adapter and used for all "measured CV" numbers
elsewhere in this repo.

### Why a second generation

The original 640px model was deployed via 2×2 tiling at 1280px for
inference, which fed it out-of-distribution crops (people rendered ~3.7×
larger than anything seen in training) and silently capped recall at the
actual deployment configuration. Retraining natively at 1280px, on a larger
and more diverse dataset, fixed this: overall recall at the deployment
config went from **71.8% → 95.1%**, and sub-8px recall from **62.5% → 89.9%**.

### Dataset size

| Split | Images | Survivor annotations | Negative (no person) | With fire/smoke | With ≥1 decoy |
|-------|:------:|:--------------------:|:---------------------:|:---------------:|:-------------:|
| Train | 6,000 | 10,462 | 704 (11.7%) | 4,511 (75.2%) | 2,235 (37.3%) |
| Val | 800 | 1,417 | 85 (10.6%) | 615 (76.9%) | 271 (33.9%) |
| **Total** | **6,800** | **11,879** | **789 (11.6%)** | **5,126 (75.4%)** | **2,506 (36.9%)** |

### What changed vs. the original 2,400-image set (§1–13)

| Change | Original (§1–13) | 1280px retrain (this section) |
|---|---|---|
| Images | 2,400 | 6,800 (2.8×) |
| Resolution | 640×640 | 1280×1280 (native, no tiling mismatch) |
| Backgrounds | Malibu NAIP + some non-wildfire tiles | Malibu-only NAIP, wildfire-terrain-biased sampling |
| Hard negatives | Vehicles only (initially) | Vehicles + **animal** + **colourful-object** decoys, sized 0.5–1.8 m like survivors |
| Fire/smoke frac | 65% | 75% (`--fire-frac`, ground-truth-biased toward burned terrain) |
| Person model | 2.4 m square blob (early) → oriented rectangle (later fix, carried forward) | Same oriented rectangle (0.55 m shoulder × 1.75 m body), alpha-tight boxes |

### Altitude & size distribution

| Metric | Value |
|--------|-------|
| Altitude range | 20.0–50.0 m (mean 35.0 m) |
| GSD range | 0.040–0.100 m/px |
| Oblique views | 24.7% (15–45° tilt) |
| Box long-axis <8 px | 2,551 (21.5%) |
| Box long-axis 8–15 px | 5,572 (46.9%) |
| Box long-axis >15 px | 3,756 (31.6%) |
| Decoy objects: vehicle / animal / colourful | 2,509 / 1,223 / 1,266 |

Decoy composition is now close to 1:1:1 across the three classes (vs. the
original set's vehicle-heavy ~50/25/25 split), directly targeting the
reviewer's concern that vehicle-only decoys let the model shortcut on
"large plain blob = not a person" instead of learning human shape.

### Training result

| Model | Precision | Recall (synthetic val) | mAP50 |
|-------|:---------:|:-----------------------:|:-----:|
| `survivor_yolov8s_1280.pt` | 0.82 | 0.95 (0.72 clear @ deployment tiling before retrain) | — |

See `docs/cv_dataset_composition_report.md` for the reviewer-facing decoy
false-positive analysis (colourful-object FP 40.9%→14.3%, animal FP
6.7%→2.0% after this retrain) and the slide deck
(`docs/slides/omnisearch_cv_results.pptx`) for the full recall-by-size
heatmap.

---

## 16. Real-Image Datasets & Sim-to-Real Validation

All datasets above are synthetic composites. To answer the question "does
this transfer to real imagery?", five real-image (or real-terrain +
real-person-cutout) datasets were added this cycle, purely for **evaluation
and fine-tuning** — none of them feed the simulator's own rendering
pipeline. Full methodology and per-config numbers: `docs/real_data_eval_report.md`.

### 16.1 Real datasets added

| Dataset | Location | Images | What it is | Used for |
|---|---|:---:|---|---|
| HIT-UAV | `data/cv_train/thermal_real/` | 2,898 | Real drone thermal-IR frames, human-annotated | Thermal real-data eval + fine-tune |
| HERIDAL | `data/cv_train/heridal/` | 1,647 | Real ~50 m drone photos over Mediterranean wilderness, SAR-annotated | Drone RGB real-data eval + fine-tune |
| COCO person subset | `data/cv_train/ugv_real_coco/` | 5,336 | Real ground-level photos (COCO val2017, person class) | UGV real-data eval + fine-tune (generic domain) |
| FLAME 3 composites | `data/cv_train/flame_composites/` | 230 | Real SARD person cutouts pasted onto real DJI M30T wildfire-drone frames (Sycan Marsh prescribed burn), physics-sized per-pixel from XMP altitude/gimbal pitch | Drone RGB recall through **real** smoke |
| Real Malibu-terrain UGV | `data/cv_train/ugv_malibu_real/` | 185 | Real SARD person cutouts pasted onto 37 real ground-level photos of Malibu Canyon / Malibu Creek / Topanga / Point Mugu State Parks (Wikimedia Commons, CC-licensed) | UGV real-data eval — domain-matched replacement for COCO |

Source terrain photos for the last row live in
`data/cv_assets/malibu_terrain_photos/` (37 images, `SOURCES.tsv` has
per-file Commons attribution); build script:
`scripts/build_ugv_malibu_composites.py`.

### 16.2 The sim-to-real gap, measured

Recall of the **synthetic-only** models collapses when moved from their own
synthetic validation split to any real test set — then most of it is
recovered by mixing real data into training:

| Modality | Real test set | Synthetic val | Real recall before FT | Real recall after FT |
|---|---|:---:|:---:|:---:|
| Thermal | HIT-UAV test (579 img) | 96% | 31% | **86%** |
| Drone RGB | HERIDAL test (101 img, 337 GT) | 95% | 22% | **82%** |
| UGV front | Real Malibu terrain (185 img, 226 people) | 33% | 4% | **63%** |
| UGV front | COCO holdout | 33% | 1% | **57%** |

- **Every modality loses 60–95 points of recall** moving from synthetic val
  to real imagery — the gap the teammate's critique correctly anticipated.
- **Mixed real+synthetic fine-tuning closes most of it** in every modality
  (`thermal_real_yolov8n.pt`, `survivor_heridal_yolov8s_1280.pt`,
  `ugv_front_yolov8s_real.pt`), while keeping (or improving) synthetic-val
  performance — no catastrophic forgetting once real data is mixed in
  rather than swapped in.
- **UGV note:** the fine-tuned model was never shown a single wilderness
  photo during fine-tuning (only synthetic UGV composites + COCO), yet it
  generalizes *better* to real Malibu terrain (63%) than to COCO (57%) —
  the domain-matched real test is both more relevant and more encouraging
  than the generic one it replaced.

### 16.3 Real wildfire smoke (FLAME 3)

Real SARD cutouts on real Sycan Marsh prescribed-burn drone frames (not a
before/after-fine-tune comparison — both models are tested on the same real
smoke, at the deployment tiled-inference config):

| Model | Recall (tiled 4×4 @1280, conf 0.35) | Clear | Light smoke | Heavy smoke | FP / person-free fire frame |
|---|:---:|:---:|:---:|:---:|:---:|
| `survivor_yolov8s_1280` (synthetic only) | 67.5% | 66% | 68% | 81% | 8.1 |
| `survivor_heridal_yolov8s_1280` (fine-tuned) | 68.5% | 68% | 68% | 77% | **2.9** |

Recall through real smoke is comparable between the two models and
roughly flat across smoke density — the real cost of fire scenes is **false
positives**, not misses, and real-data fine-tuning cuts those ~3×.

### 16.4 Known limitations of the real-data additions

1. The real Malibu-terrain UGV set (185 images) is still a **composite** —
   real terrain, real person cutout, synthetic placement/scale — not a
   photograph of an actual person in Malibu. It removes the domain mismatch
   of COCO but not the "cutout" artifact risk that FLAME/COCO share too.
2. Wikimedia Commons photos have no camera-pose metadata, so person scale
   is derived from the UGV's own front-camera model (5–30 m range assumption)
   rather than per-photo geometry, unlike FLAME's XMP-derived altitude/pitch.
3. Sample sizes are modest for some slices (heavy-smoke FLAME bucket is
   n=26; real Malibu-terrain set is 226 people across only 37 unique
   background photos, so background diversity, not placement count, bounds
   the sample).
4. None of these real/real-composite sets are in the current Zenodo record
   (see §14).
